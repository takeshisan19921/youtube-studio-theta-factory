# Theta PyTorch SSH Image

Minimal PyTorch CUDA SSH image for Theta EdgeCloud GPU Node smoke tests.

Includes PyTorch CUDA, SSH, ffmpeg, git, curl, rsync, and tini. It does not include models, generated media, IDrive credentials, or final packages.
